<x-super-admin-layout title="Daily Journey"><div class="grid gap-6 lg:grid-cols-[1fr_2fr]"><aside class="rounded-2xl bg-white p-5 dark:bg-slate-900"><h2 class="font-extrabold">Recent game days</h2>@foreach($days as $day)<p class="mt-3 flex justify-between text-sm"><span>{{ $day->local_date->toDateString() }}</span><span>{{ $day->status }}</span></p>@endforeach</aside><section><form class="mb-4 flex gap-2"><input name="q" value="{{ request('q') }}" placeholder="Guide name or handle" class="rounded-xl dark:bg-slate-900"><select name="status" class="rounded-xl dark:bg-slate-900"><option value="">All statuses</option>@foreach(['accepted','suspicious','rejected','invalidated'] as $status)<option @selected(request('status')===$status)>{{ $status }}</option>@endforeach</select><button class="rounded-xl bg-indigo-600 px-4 text-white">Filter</button></form><div class="overflow-x-auto rounded-2xl bg-white dark:bg-slate-900"><table class="w-full text-sm"><tr><th class="p-3 text-left">Run</th><th>Guide</th><th>Score</th><th>Status</th><th></th></tr>@foreach($runs as $run)<tr class="border-t dark:border-slate-800"><td class="p-3">#{{ $run->id }}</td><td>{{ $run->user->publicName() }}</td><td>{{ number_format($run->score) }}</td><td>{{ $run->validation_status }}</td><td><a href="{{ route('super-admin.game.show',$run) }}" class="text-indigo-500">Review</a></td></tr>@endforeach</table></div>{{ $runs->links() }}</section></div></x-super-admin-layout>

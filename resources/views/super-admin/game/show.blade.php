<x-super-admin-layout title="Review game run"><a href="{{ route('super-admin.game.index') }}" class="text-indigo-500">← Runs</a><div class="mt-5 rounded-2xl bg-white p-6 dark:bg-slate-900"><h2 class="text-2xl font-extrabold">Run #{{ $run->id }}</h2><dl class="mt-5 grid gap-4 sm:grid-cols-3">@foreach(['validation_status','score','distance','duration_ms','collectible_count','powerup_pickup_count','powerup_use_count','maximum_speed_tier','client_version'] as $field)<div><dt class="text-xs uppercase text-slate-500">{{ str_replace('_',' ',$field) }}</dt><dd class="font-bold">{{ $run->{$field} }}</dd></div>@endforeach</dl><pre class="mt-5 overflow-auto rounded-xl bg-slate-950 p-4 text-xs text-slate-200">{{ json_encode($run->validation_flags, JSON_PRETTY_PRINT) }}</pre>@if(!$run->invalidated_at)<form method="POST" action="{{ route('super-admin.game.invalidate',$run) }}" class="mt-6">@csrf @method('PATCH')<label class="block font-bold">Invalidation reason <textarea name="reason" required minlength="10" class="mt-2 block w-full rounded-xl dark:bg-slate-950"></textarea></label><button class="mt-3 rounded-xl bg-rose-600 px-5 py-3 font-bold text-white">Invalidate run</button></form>@endif</div></x-super-admin-layout>

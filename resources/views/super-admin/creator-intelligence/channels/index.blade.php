<x-creator-intelligence-layout title="Creator Channels">
    <div class="mb-5 flex items-center justify-between"><h2 class="text-2xl font-extrabold">Creator Channels</h2><a href="{{ route('superadmin.creator-intelligence.channels.create') }}" class="ci-button-primary">Add Channel</a></div>
    <div class="ci-table-container"><table class="min-w-full divide-y divide-slate-200 dark:divide-slate-800"><thead><tr class="text-left text-xs uppercase tracking-wide"><th scope="col" class="p-4">Channel</th><th scope="col" class="p-4">Profile</th><th scope="col" class="p-4">Platform</th><th scope="col" class="p-4">Status</th><th scope="col" class="p-4 text-right">Actions</th></tr></thead><tbody class="divide-y divide-slate-200 dark:divide-slate-800">@forelse($channels as $channel)<tr><td class="p-4 font-bold">{{ $channel->channel_name }}</td><td class="p-4">{{ $channel->profile->display_name }}</td><td class="p-4">{{ str($channel->platform)->headline() }}</td><td class="p-4"><span class="{{ $channel->is_active ? 'ci-badge-success' : 'ci-badge-warning' }}">{{ $channel->is_active ? 'Active' : 'Inactive' }}</span></td><td class="p-4 text-right"><a class="font-bold text-indigo-700 dark:text-indigo-300" href="{{ route('superadmin.creator-intelligence.channels.edit', $channel) }}">Edit Channel</a></td></tr>@empty<tr><td colspan="5" class="p-8 text-center text-slate-600 dark:text-slate-300">No creator channels yet.</td></tr>@endforelse</tbody></table></div>
    <div class="mt-5 text-slate-700 dark:text-slate-200">{{ $channels->links() }}</div>
</x-creator-intelligence-layout>

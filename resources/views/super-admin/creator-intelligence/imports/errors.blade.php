<x-creator-intelligence-layout title="Failed Import Rows">
    <div class="flex flex-wrap items-center justify-between gap-3"><h2 class="text-2xl font-extrabold">Failed rows for import #{{ $batch->id }}</h2><a href="{{ route('superadmin.creator-intelligence.imports.failed-rows',$batch) }}" class="ci-button-secondary">Download CSV</a></div>
    <div class="ci-table-container mt-5" tabindex="0" aria-label="Scrollable failed import rows"><table class="min-w-full text-sm"><thead><tr><th scope="col" class="p-3 text-left">Row</th><th scope="col" class="p-3 text-left">Message</th><th scope="col" class="p-3 text-left">Raw data</th></tr></thead><tbody>@foreach($rows as $row)<tr class="border-t"><td class="p-3">{{ $row->row_number }}</td><td class="p-3">{{ $row->message }}</td><td class="p-3"><pre class="max-w-3xl whitespace-pre-wrap text-xs">{{ json_encode($row->raw_data,JSON_PRETTY_PRINT) }}</pre></td></tr>@endforeach</tbody></table></div>
    <div class="mt-4">{{ $rows->links() }}</div>
</x-creator-intelligence-layout>

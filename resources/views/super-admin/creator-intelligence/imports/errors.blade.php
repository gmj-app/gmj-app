<x-creator-intelligence-layout title="Failed Import Rows"><div class="flex items-center justify-between"><h2 class="text-2xl font-extrabold">Failed rows for import #{{ $batch->id }}</h2><a href="{{ route('superadmin.creator-intelligence.imports.failed-rows',$batch) }}" class="font-bold text-indigo-600">Download CSV</a></div><div class="mt-5 overflow-x-auto rounded-2xl border border-slate-200 dark:border-slate-800"><table class="min-w-full text-sm"><thead><tr><th class="p-3 text-left">Row</th><th class="p-3 text-left">Message</th><th class="p-3 text-left">Raw data</th></tr></thead><tbody>@foreach($rows as $row)<tr class="border-t border-slate-200 dark:border-slate-800"><td class="p-3">{{ $row->row_number }}</td><td class="p-3">{{ $row->message }}</td><td class="p-3"><pre class="max-w-3xl whitespace-pre-wrap text-xs">{{ json_encode($row->raw_data,JSON_PRETTY_PRINT) }}</pre></td></tr>@endforeach</tbody></table></div><div class="mt-4">{{ $rows->links() }}</div></x-creator-intelligence-layout>

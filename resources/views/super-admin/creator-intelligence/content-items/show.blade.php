<x-creator-intelligence-layout :title="$item->name"><div class="mb-5 flex flex-wrap justify-between gap-3"><div><p class="ci-muted">{{ $contentItemLabel }}</p><h2 class="text-2xl font-extrabold">{{ $item->name }}</h2><p class="ci-muted mt-1">{{ $item->creatorChannel->channel_name }} · {{ $item->subject?->name ?? 'No '.strtolower($subjectLabel) }} · {{ $item->videos_count }} videos</p></div><a class="ci-button-secondary" href="{{ route('superadmin.creator-intelligence.content-items.edit', $item) }}">Edit {{ $contentItemLabel }}</a></div><div class="ci-card"><h3 class="font-bold">Videos</h3>@forelse($videos as $video)<a class="block py-2 font-semibold text-indigo-700 dark:text-indigo-300" href="{{ route('superadmin.creator-intelligence.videos.show', $video) }}">{{ $video->title }}</a>@empty<p class="ci-muted">No videos assigned.</p>@endforelse</div><div class="mt-5 text-slate-700 dark:text-slate-200">{{ $videos->links() }}</div></x-creator-intelligence-layout>

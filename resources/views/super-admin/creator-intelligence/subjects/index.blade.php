<x-creator-intelligence-layout :title="$labels->subjectPlural()">
    <div class="mb-5 flex flex-wrap items-center justify-between gap-3"><h2 class="text-2xl font-extrabold">{{ $labels->subjectPlural() }}</h2><a href="{{ route('superadmin.creator-intelligence.subjects.create', array_filter(['creator_channel_id'=>$selectedChannel?->id])) }}" class="ci-button-primary">Add {{ $labels->subject }}</a></div>
    <form class="ci-filter-panel mb-5 grid gap-4 sm:grid-cols-[minmax(0,1fr)_minmax(12rem,18rem)_auto]"><label for="subject-search" class="ci-label">Search {{ $labels->subjectPlural() }}<input id="subject-search" name="q" value="{{ request('q') }}" placeholder="Search {{ $labels->lowerSubjects() }}" class="ci-control mt-1 w-full"></label><label for="subject-channel-filter" class="ci-label">Creator Channel<select id="subject-channel-filter" name="creator_channel_id" class="ci-control mt-1 w-full"><option value="">All channels</option>@foreach($channels as $channel)<option value="{{ $channel->id }}" @selected(request('creator_channel_id') == $channel->id)>{{ $channel->channel_name }}</option>@endforeach</select></label><button class="ci-button-primary self-end">Apply Filters</button></form>
    <div class="ci-table-container"><table class="min-w-full"><thead><tr><th scope="col" class="p-4 text-left">{{ $labels->subject }}</th><th scope="col" class="p-4 text-left">Channel</th><th scope="col" class="p-4 text-right">Videos</th><th scope="col" class="p-4 text-right">Actions</th></tr></thead><tbody class="divide-y divide-slate-200 dark:divide-slate-800">@forelse($subjects as $subject)<tr><td class="p-4 font-bold"><a class="text-indigo-700 dark:text-indigo-300" href="{{ route('superadmin.creator-intelligence.subjects.show', $subject) }}">{{ $subject->name }}</a></td><td class="p-4">{{ $subject->creatorChannel->channel_name }}</td><td class="p-4 text-right tabular-nums">{{ $subject->videos_count }}</td><td class="p-4 text-right"><a class="font-bold text-indigo-700 dark:text-indigo-300" href="{{ route('superadmin.creator-intelligence.subjects.edit', $subject) }}">Edit {{ $subject->creatorChannel->subject_label }}</a></td></tr>@empty<tr><td colspan="4" class="p-8 text-center text-slate-600 dark:text-slate-300">No {{ $labels->lowerSubjects() }} found.</td></tr>@endforelse</tbody></table></div><div class="mt-5 text-slate-700 dark:text-slate-200">{{ $subjects->links() }}</div>
</x-creator-intelligence-layout>

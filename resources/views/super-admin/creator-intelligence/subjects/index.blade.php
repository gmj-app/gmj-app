<x-creator-intelligence-layout title="Subjects">
    <div class="mb-5 flex items-center justify-between"><h2 class="text-2xl font-extrabold">Subjects</h2><a href="{{ route('superadmin.creator-intelligence.subjects.create') }}" class="rounded-xl bg-indigo-600 px-4 py-2 font-bold text-white">Add subject</a></div>
    <form class="mb-5 flex gap-3"><input name="search" value="{{ request('search') }}" placeholder="Search subjects" class="rounded-xl border-slate-300"><select name="creator_channel_id" class="rounded-xl border-slate-300"><option value="">All channels</option>@foreach($channels as $channel)<option value="{{ $channel->id }}" @selected(request('creator_channel_id') == $channel->id)>{{ $channel->channel_name }}</option>@endforeach</select><button class="rounded-xl bg-slate-900 px-4 py-2 text-white">Filter</button></form>
    <div class="overflow-x-auto rounded-2xl border bg-white"><table class="min-w-full"><thead><tr class="text-left"><th class="p-4">Subject</th><th class="p-4">Channel</th><th class="p-4">Videos</th><th></th></tr></thead><tbody>@forelse($subjects as $subject)<tr class="border-t"><td class="p-4 font-bold"><a href="{{ route('superadmin.creator-intelligence.subjects.show', $subject) }}">{{ $subject->name }}</a></td><td class="p-4">{{ $subject->creatorChannel->channel_name }}</td><td class="p-4">{{ $subject->videos_count }}</td><td class="p-4"><a href="{{ route('superadmin.creator-intelligence.subjects.edit', $subject) }}">Edit</a></td></tr>@empty<tr><td colspan="4" class="p-8 text-center">No subjects found.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $subjects->links() }}</div>
</x-creator-intelligence-layout>

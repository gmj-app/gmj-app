<x-creator-intelligence-layout :title="$subject->name"><div class="mb-5 flex flex-wrap justify-between gap-3"><div><p class="ci-muted">{{ $subjectLabel }}</p><h2 class="text-2xl font-extrabold">{{ $subject->name }}</h2><p class="ci-muted mt-1">{{ $subject->creatorChannel->channel_name }} · {{ $subject->videos_count }} videos · {{ $subject->content_items_count }} {{ strtolower(str($contentItemLabel)->plural()) }}</p></div><a class="ci-button-secondary" href="{{ route('superadmin.creator-intelligence.subjects.edit', $subject) }}">Edit {{ $subjectLabel }}</a></div><div class="ci-card"><h3 class="font-bold">Videos</h3>@forelse($videos as $video)<a class="mt-3 block font-semibold text-indigo-700 dark:text-indigo-300" href="{{ route('superadmin.creator-intelligence.videos.show', $video) }}">{{ $video->title }}</a>@empty<p class="ci-muted mt-3">No videos assigned.</p>@endforelse</div><div class="mt-5 text-slate-700 dark:text-slate-200">{{ $videos->links() }}</div></x-creator-intelligence-layout>

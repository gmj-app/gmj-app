<x-creator-intelligence-layout :title="$subject->name"><div class="mb-5 flex justify-between"><div><h2 class="text-2xl font-extrabold">{{ $subject->name }}</h2><p>{{ $subject->creatorChannel->channel_name }} · {{ $subject->videos_count }} videos · {{ $subject->content_items_count }} content items</p></div><a href="{{ route('superadmin.creator-intelligence.subjects.edit', $subject) }}">Edit</a></div><div class="rounded-2xl border bg-white p-5"><h3 class="font-bold">Videos</h3>@forelse($videos as $video)<a class="mt-3 block text-indigo-600" href="{{ route('superadmin.creator-intelligence.videos.show', $video) }}">{{ $video->title }}</a>@empty<p class="mt-3">No videos assigned.</p>@endforelse</div><div class="mt-5">{{ $videos->links() }}</div></x-creator-intelligence-layout>

<x-app-layout><x-slot name="title">Notifications</x-slot>
<div class="py-10 sm:py-12"><div class="mx-auto max-w-5xl px-4 sm:px-6 lg:px-8">
    @if(session('success'))<div role="status" aria-live="polite" class="mb-5 rounded-xl bg-emerald-100 p-4 font-bold text-emerald-800 dark:bg-emerald-950 dark:text-emerald-200">{{ session('success') }}</div>@endif
    <div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-xs font-extrabold uppercase tracking-[0.18em] text-indigo-600">Your account</p><h1 class="mt-1 text-3xl font-extrabold">Notifications</h1><p class="mt-2 text-slate-600 dark:text-slate-300">Updates about your requests, creator activity, achievements, and account.</p></div><form method="POST" action="{{ route('notifications.read-all') }}">@csrf<button class="rounded-xl bg-indigo-600 px-4 py-2 font-bold text-white">Mark all as read</button></form></div>
    <nav aria-label="Notification filters" class="mt-6 flex gap-2 overflow-x-auto pb-2"><a href="{{ route('notifications.index') }}" class="shrink-0 rounded-full px-4 py-2 text-sm font-bold {{ $filter==='all' && $category==='' ? 'bg-indigo-600 text-white' : 'bg-slate-100 dark:bg-slate-800' }}">All</a><a href="{{ route('notifications.index',['filter'=>'unread']) }}" class="shrink-0 rounded-full px-4 py-2 text-sm font-bold {{ $filter==='unread' ? 'bg-indigo-600 text-white' : 'bg-slate-100 dark:bg-slate-800' }}">Unread</a>@foreach(config('notifications.categories') as $key=>$label)<a href="{{ route('notifications.index',['category'=>$key]) }}" class="shrink-0 rounded-full px-4 py-2 text-sm font-bold {{ $category===$key ? 'bg-indigo-600 text-white' : 'bg-slate-100 dark:bg-slate-800' }}">{{ $label }}</a>@endforeach</nav>
    <div class="mt-5 space-y-3">@php($notificationDateGroup = null)@forelse($notifications as $item)@if($item->dateGroup() !== $notificationDateGroup)@php($notificationDateGroup = $item->dateGroup())<h2 class="pt-3 text-sm font-extrabold uppercase tracking-wide text-slate-500 dark:text-slate-400">{{ $notificationDateGroup }}</h2>@endif<x-notifications.item :item="$item" />@empty<div class="rounded-2xl border border-dashed border-slate-300 p-10 text-center"><p class="font-extrabold">{{ $filter==='unread' ? 'No unread notifications.' : ($category ? 'No '.strtolower(config('notifications.categories.'.$category)).' notifications yet.' : 'No notifications yet.') }}</p></div>@endforelse</div>
    <div class="mt-6">{{ $notifications->links() }}</div>
</div></div></x-app-layout>
